# 体检评价与健康档案系统测试报告

- 测试环境：Windows、Python 3.12、Node.js、SQLite
- 后端：Flask + SQLAlchemy + JWT + Waitress
- 前端：Vue 3 + Vite + Element Plus + ECharts
- OCR：Mock 和真实 API 两种配置模式
- 结论：通过

## 自动化测试

后端命令：

```powershell
Set-Location .\backend
.\.venv\Scripts\python.exe -m pytest -q
```

结果：`39 passed`

覆盖内容：

- 注册、登录、验证码、刷新令牌和鉴权。
- 机构和套餐查询。
- 档案 CRUD、指标 CRUD 和异常值判断。
- OCR 解析、候选映射与确认入档。
- 亲友关系、授权与代传权限。
- 指标趋势和统计结果。
- 评论提交、审核、展示和删除。
- 管理员用户管理。
- SQLite 模型约束和本地配置。
- SQLite 外键开关和真实建表 DDL 中的命名约束。
- CHECK、外键和候选键唯一约束的实际拒绝行为。

## 前端构建

```powershell
Set-Location .\frontend
npm run build
```

结果：构建成功。

## 本地集成测试

| 项目 | 结果 |
|---|---|
| Flask 开发入口启动 | 通过 |
| Waitress 入口启动 | 通过 |
| `/api/health` | `200 / {"status":"ok"}` |
| Vite 开发页 | `200` |
| Vite `/api` 代理 | 通过 |
| Vite 生产构建 | 通过 |
| 原有 SQLite 数据读取 | 通过 |
| 旧数据库结构安全升级 | 通过 |
| 25 个缺失约束补齐 | 通过 |
| 外键完整性检查 | 0 个违规 |
| SQLite `integrity_check` | `ok` |

## 功能测试矩阵

| 用例 | 场景 | 预期结果 |
|---|---|---|
| TC-01 | 注册与登录 | 用户可完成认证 |
| TC-02 | 错误密码 | 返回 401 |
| TC-03 | 图片验证码 | 错误答案被拒绝 |
| TC-04 | 机构和套餐 | 列表与详情正确显示 |
| TC-05 | 手动创建档案 | 档案和指标成功保存 |
| TC-06 | 重复指标 | 唯一约束阻止重复 |
| TC-07 | 异常指标 | 自动标记异常 |
| TC-08 | OCR 上传 | 返回解析候选 |
| TC-09 | OCR 确认 | 确认后指标正式入档 |
| TC-10 | 未授权代传 | 请求被拒绝 |
| TC-11 | 已授权代传 | 档案成功归属亲友 |
| TC-12 | 趋势分析 | 返回时序点和统计值 |
| TC-13 | 评论门槛 | 无机构档案时禁止评论 |
| TC-14 | 评论审核 | 管理员可控制公开状态 |
| TC-15 | 用户管理 | 管理员可修改和删除用户 |

## 数据隔离

自动化测试使用内存 SQLite。测试过程中不会读取或修改：

```text
backend/instance/health_system.db
```

## 数据库结构升级验证

升级前审计发现旧 SQLite 物理表缺少 25 个模型中已有的命名约束：

- 22 个 CHECK 约束。
- 3 个候选键联合唯一约束。

数据违规预检结果均为 0，随后执行本地安全重建。升级后：

- 所有原有业务表行数保持一致。
- 缺失命名约束为 0。
- 外键违规为 0。
- 数据库完整性检查为 `ok`。
- 自动生成升级前数据库备份。
