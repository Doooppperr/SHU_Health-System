# PDF 交付物待更新清单

- 核对日期：2026-07-13
- 本轮范围：只记录 PDF 与当前已实现系统的差异，不重制、不覆盖现有 PDF。

| PDF | 与当前实现的主要差异 | 后续重制时需要同步 |
|---|---|---|
| 数据库2报告.pdf | 仍以旧数据库方案和旧角色/机构优先结构为主，包含已不再适用的 GaussDB 叙述；未反映 SQLite schema v2、三角色约束、机构管理员绑定、软停用、邀请码/相册、档案机构可空，也未说明 `healthN` 为派生展示值、OCR 待确认快照和 AI/分析结果不落库。 | 更新技术选型、schema v2 表结构、约束和关系图；补充安全重建、备份与原子替换；把 2/3/15/12 标为 2026-07-11 迁移快照；补充 OCR 两阶段暂存/CAS 与 AI 无业务表的边界，以及 117/97 和 CI 结果。 |
| 视频脚本.pdf | 仍从直接登录页和机构主线开始，未展示公开门户、独立登录/注册、按角色跳转和三套工作台；未覆盖邀请码、只读脱敏数据、机构相册、可选档案来源、原始报告保护，以及最新 AI SSE/按需选档/智能分析、现有档案 OCR、`healthN` 展示编号和侧栏缩放。 | 重排为“公开门户 → 普通用户健康档案 → 新建档案并 OCR 补传 → AI 普通问答/按需选档/单多档分析 → 系统管理员签发邀请码 → 机构管理员后台 → 撤销与权限隔离”；演示桌面缩放和窄屏 overlay，并补充逐请求同意、并发冲突和隐私说明。 |

现有 PDF 保持原文件不变；待收到单独重制指令后，再依据当前 Markdown 文档和实际页面生成新版。
